application:
  name: Application Runner
modes:
  interactive:
    menus:
      - main
      - test
      - test1
log_settings:
  level: info
  dir_path: logs
  max_size: 10 KB
  file_name: main
  msg_format: '{level} | {date} | {msg}'
  date_format: '%Y-%m-%d %H:%M:%S'