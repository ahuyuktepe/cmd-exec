application:
  name: Application Runner 123
log_settings:
  level: info
  dir_path: logs
  max_size: 10 KB
  file_name: main
  msg_format: '{level} | {date} | {msg}'
  date_format: '%Y-%m-%d %H:%M:%S'
command_locators:
  add-web-tool:
    cmd: add-web-tool
    module: main
    menu: test
    arguments:
      name: Default Name